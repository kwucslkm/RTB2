<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.project.mapper.UserMapper">
 	<!--  전체 사용자 조회 -->
    <!--<select id="findAll" resultType="com.example.project.entity.User">
        SELECT * FROM users
    </select>
     사용자 저장
    <insert id="save" parameterType="com.example.project.entity.User" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO users (email, password, name) 
        VALUES (#{email}, #{password}, #{name})
    </insert> -->
</mapper>
